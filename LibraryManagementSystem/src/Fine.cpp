#include "Fine.h"
#include <iostream>

void Fine::collectFine(const string& memberId, long days) {
    // Dummy print to simulate fine collection
    std::cout << "Collecting fine from " << memberId << " for " << days << " days.\n";
}
