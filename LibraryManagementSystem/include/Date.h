#ifndef DATE_H
#define DATE_H

class Date {
public:
    // Dummy implementation - you can enhance later
    long getTime() const { return 0; }
    int compareTo(const Date& other) const { return 0; }
};

#endif
